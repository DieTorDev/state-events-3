import styled from 'styled-components';

const StyledMainContainer = styled.main`
	max-width: 400px;
	margin: auto;
`;

const StyledUserContainer = styled.section`
	display: flex;
	flex-direction: column;
	gap: 10px;
`;

export { StyledMainContainer, StyledUserContainer };
